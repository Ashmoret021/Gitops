ashmoret-helm:  
  objects:

    - name: frontend
      type: deployment
      tag: latest
      replicas: 2

      service:
        enabled: true
        port: 80
        targetPort: 80

      configMap:
        enabled: true
        data:
          APP_ENV: production
